name: 自动验收 (Auto Check)

on: [push]

jobs:
  docker-build-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: 🔍 验证配置有效性 (Config Check)
        run: docker compose config
        
      - name: 🔍 测试 Docker 构建 (Build Test)
        # 使用新版命令 docker compose (v2)
        run: docker compose build